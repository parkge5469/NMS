module.exports = (error) => {
    const error_LV = error.error_LV;

    if('nomal'===error_LV) {
        
    } else if('warning'===error_LV) {
         
    } else if('faital'===error_LV) { 

    }
    
}